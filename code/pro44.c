#include<stdio.h>
int main(){
    char name[20],ad[100];
    int age,no,i;
    // printf("entre your name:");
    // scanf("%s",&name);
    // printf("entre your adress:");
    // scanf("%s",&ad);
    // printf("entre your age:");
    // scanf("%d",&age);
    // printf("entre your number:");
    // scanf("%d",&no);
    // printf("\n your name is %s your adres is %s your age is %d your number is %d",name,ad,age,no);
    for(i=0;i<=name[i];i++){
    printf("entre your name:");
    scanf("%s",&name);
    printf("entre your adress:");
    scanf("%s",&ad);
    printf("entre your age:");
    scanf("%d",&age);
    printf("entre your number:");
    scanf("%d",&no);
    printf("\n your name is %s your adres is %s your age is %d your number is %d",name,ad,age,no);
  }
   for ( i = 0; i < name[i]; i++)
   {
    /* code */
    printf("\n your name is %s your adres is %s your age is %d your number is %d",name,ad,age,no);
   }
   
}